name: Deploy Leaderboard Worker

on:
  push:
    branches:
      - main
    paths:
      - 'workers/leaderboard-worker.js'
      - 'workers/leaderboard/**'
      - '.github/workflows/deploy-leaderboard-worker.yml'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Cloudflare Workers
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: workers/leaderboard
          command: deploy
