# Block Topia Unified Assets

This directory is the unified module asset home.

Current policy:
- Reuse the already merged Street Signal 3008 pack as source-of-truth inputs.
- Keep references compatible with `/games/data/blocktopia-asset-pack.json` and existing SVG sets during transition.
- Add future native assets here without replacing legacy reference files.
