export const titleScreen = {
  title: 'NBG LONDON GRAFFITI RUN',
  subtitle: 'RUN • TAG • EARN XP',
  startButton: 'START LEVEL 1',
  leaderboardButton: 'LEADERBOARD'
};

export function startGame(callback) {
  if (callback) callback();
}
