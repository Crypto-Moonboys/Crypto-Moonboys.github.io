/**
 * config.js — Breakout Bullrun game metadata and configuration.
 */

export var BREAKOUT_CONFIG = Object.freeze({
  /** Leaderboard / ArcadeSync key. */
  id: 'breakout',

  /** Display label used by GameRegistry. */
  label: '🧱 Breakout Bullrun',
});
