/**
 * config.js — Block Topia Quest Maze game metadata and configuration.
 */

export var BLOCKTOPIA_QUEST_MAZE_CONFIG = Object.freeze({
  /** Leaderboard / ArcadeSync key. */
  id: 'blocktopia',

  /** Display label used by GameRegistry. */
  label: '🧱 Block Topia Quest Maze',

  /** Phaser canvas container element ID. */
  canvasId: 'btqm-canvas',
});
