/**
 * config.js — HexGL Monster Mode game metadata and configuration.
 */

export var HEXGL_MONSTER_CONFIG = Object.freeze({
  /** Leaderboard / ArcadeSync key. */
  id: 'hexgl',

  /** Display label used by GameRegistry. */
  label: '🏁 HexGL — Monster Mode',

  /** Upstream HexGL URL served via jsDelivr CDN. */
  src: 'https://cdn.jsdelivr.net/gh/BKcore/HexGL@master/index.html',
});
