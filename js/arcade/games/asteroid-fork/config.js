/**
 * config.js — Forkfield game metadata and configuration.
 */

export var ASTEROID_FORK_CONFIG = Object.freeze({
  /** Stable leaderboard / ArcadeSync compatibility key. */
  id: 'asteroids',

  /** Display label used by GameRegistry. */
  label: '🌑 Forkfield',

  /** Cross-game modifier compatibility tags. */
  crossGameTags: Object.freeze(['shooter', 'physics']),
});
