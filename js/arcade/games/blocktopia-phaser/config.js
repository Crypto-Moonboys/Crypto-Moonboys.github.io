/**
 * config.js — Block Topia: Street Signal 3008 (Phaser) metadata and configuration.
 */

export var BLOCKTOPIA_PHASER_CONFIG = Object.freeze({
  /** Leaderboard / ArcadeSync key. */
  id: 'blocktopia',

  /** Display label used by GameRegistry. */
  label: '🎮 Block Topia: Street Signal 3008 (Phaser)',

  /** Phaser-root container element ID used in each host page. */
  containerId: 'phaser-root',

  /** Loading overlay element ID (present on monster variant; null if absent). */
  loadingId: 'bt-loading',

  /** Delay in ms before hiding the loading overlay after boot. */
  loadHideDelayMs: 1800,
});
