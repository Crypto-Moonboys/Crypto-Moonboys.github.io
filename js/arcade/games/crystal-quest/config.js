/**
 * config.js — Crystal Quest game metadata and configuration.
 */

export var CRYSTAL_QUEST_CONFIG = Object.freeze({
  /** Leaderboard / ArcadeSync key. */
  id: 'crystal',

  /** Display label used by GameRegistry. */
  label: '🧩 Crystal Quest',
});
