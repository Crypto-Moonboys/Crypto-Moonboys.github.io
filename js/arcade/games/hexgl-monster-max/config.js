/**
 * config.js — HexGL Monster Mode Max game metadata and configuration.
 */

export var HEXGL_MONSTER_MAX_CONFIG = Object.freeze({
  /** Leaderboard / ArcadeSync key. */
  id: 'hexgl',

  /** Display label used by GameRegistry. */
  label: '🏁 HexGL Monster Mode',

  /** Minimum valid run duration in milliseconds (anti-cheat). */
  minRunMs: 30 * 1000,
});
